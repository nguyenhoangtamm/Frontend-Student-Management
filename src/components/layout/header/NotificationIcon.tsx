'use client';
import React from 'react';

const NotificationIcon: React.FC = () => {
  return (
    <div className="d-flex align-items-center me-4" style={{ cursor: 'pointer' }}>
      <span className="text-secondary">Thông báo</span>
    </div>
  );
};

export default NotificationIcon;
